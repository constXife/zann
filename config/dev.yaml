auth:
  mode: internal
  internal:
    enabled: true
    registration: open
  oidc:
    enabled: false

server:
  master_key_mode: auto_generate
  master_key_file: "/data/smk"

metrics:
  enabled: true
  profile: debug
  endpoint: "/metrics"
