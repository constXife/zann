# Roadmap

- Custom types defined via server-side config (based on kv items): JSON Schema validation, UI hints (masking/copyable/placeholders), and API exposure.
- Mutual TLS (mTLS) support for server authentication.
